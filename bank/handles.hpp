#ifndef HANDLES_HPP
#define HANDLES_HPP

#include <iostream>

using namespace std;

namespace handles {
    string create(short);
    string modify(short);
    string close_or_enquire(short);
    string deposite_or_withdraw(short);
}

#endif